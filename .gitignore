.DS_Store
dist/
node_modules/

# the cartridge build
gba/*.o
gba/*.elf
gba/hosttest
gba/shots/*.ppm
# intermediate render output; the PNGs beside them are the keepers
gba/shots-ladder/
gba/playtest
gba/playtest_dbg
# the same sweep built with every read and write watched
gba/playtest-watched
gba/audit
gba/w

# the copy at the root that gets handed over; gba/thronebound.gba is the one
# the build writes and the one that is kept
/Thronebound.gba
# clang writes these while compiling and removes them again
gba/*.o.tmp
gba/emu/run
